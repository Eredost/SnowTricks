<h1>Bonjour {{ email.toName }}!</h1>

<p>
    Vous avez demandé une réinitialisation du mot de passe pour l'e-mail suivant :
</p>
<p><code>{{ email.to[0].address }}</code></p>

<p>
    <a href="{{ absolute_url(path('app_reset-password', {'token': user.token, 'email': user.email})) }}">
        Cliquez ici pour réinitialiser le mot de passe
    </a>
    (ce lien est valide 24 heures)
</p>
