<h1>Bienvenue {{ email.toName }}!</h1>

<p>
    Vous vous êtes inscrit avec l'adresse e-mail suivante :
</p>
<p><code>{{ email.to[0].address }}</code></p>

<p>
    <a href="{{ absolute_url(path('user_validation', {'token': user.token, 'email': user.email})) }}">
        Cliquez ici pour activer votre compte
    </a>
    (ce lien est valide 24 heures)
</p>
